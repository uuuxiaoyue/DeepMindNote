<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.web.WebView?>
<?import javafx.geometry.Insets?>

<SplitPane dividerPositions="0.2, 0.8" prefHeight="700.0" prefWidth="1100.0" xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.deepmind.controller.MainController">
    <items>
        <VBox minWidth="200" maxWidth="400" spacing="10" style="-fx-background-color: #f8f9fa; -fx-padding: 10;">
            <Label text="我的分类" style="-fx-font-weight: bold; -fx-text-fill: #495057;"/>
            <TreeView fx:id="categoryTree" VBox.vgrow="ALWAYS" />

            <Separator />

            <HBox alignment="CENTER_LEFT" spacing="5">
                <Label text="笔记列表" style="-fx-font-weight: bold;"/>
                <Region HBox.hgrow="ALWAYS" />
                <Button onAction="#handleNewNote" text="+" style="-fx-background-color: #007bff; -fx-text-fill: white; -fx-background-radius: 10;"/>
            </HBox>
            <TextField fx:id="searchField" promptText="🔍 搜索..." />
            <ListView fx:id="noteListView" VBox.vgrow="ALWAYS" />
        </VBox>

        <VBox spacing="10" style="-fx-padding: 10; -fx-background-color: white;">
            <HBox spacing="10" alignment="CENTER_RIGHT">
                <ToggleButton fx:id="btnEdit" onAction="#handleEditMode" selected="true" text="编辑">
                    <toggleGroup><ToggleGroup fx:id="modeGroup" /></toggleGroup>
                </ToggleButton>
                <ToggleButton fx:id="btnPreview" onAction="#handlePreviewMode" text="预览" toggleGroup="$modeGroup" />
                <Separator orientation="VERTICAL" />
                <Button onAction="#handleSave" style="-fx-background-color: #28a745; -fx-text-fill: white; -fx-font-weight: bold;" text="保存" />
                <Button fx:id="toggleOutlineBtn" onAction="#toggleOutline" text="📑" style="-fx-background-color: #e9ecef;" />
            </HBox>

            <StackPane VBox.vgrow="ALWAYS">
                <TextArea fx:id="editorArea" wrapText="true" style="-fx-font-family: 'Consolas', monospace; -fx-font-size: 15px;" />
                <WebView fx:id="webView" visible="false" />
            </StackPane>

            <HBox alignment="CENTER_RIGHT">
                <Label fx:id="wordCountLabel" text="字数: 0" style="-fx-text-fill: #adb5bd;" />
            </HBox>
        </VBox>

        <VBox fx:id="outlineContainer" minWidth="0" maxWidth="250" spacing="10" style="-fx-padding: 10; -fx-background-color: #fdfdfd; -fx-border-color: #dee2e6; -fx-border-width: 0 0 0 1;">
            <Label text="文章大纲" style="-fx-font-weight: bold; -fx-text-fill: #6c757d;"/>
            <ListView fx:id="outlineListView" VBox.vgrow="ALWAYS" />
        </VBox>
    </items>
</SplitPane>